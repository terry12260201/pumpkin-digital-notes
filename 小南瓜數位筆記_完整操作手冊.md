---
title: 小南瓜數位筆記 — 完整操作手冊
date: 2026-08-18
tags: [操作手冊, 影片筆記, 自動化, Claude, 交接文件]
type: knowledge
audience: Ann（可直接把這份丟給你自己的 Claude 照做）
---

# 小南瓜數位筆記 🎃 — 完整操作手冊

> 這份手冊教你：把任何一支影片（YouTube／錄影檔／要登入的線上課程），
> 變成一份**國小生也讀得懂的白話圖文報告**，還能上線分享、存進筆記庫。
> **給 Ann**：你不用會寫程式。在你自己電腦開 Claude（Claude Code），
> 把這份手冊整份丟給它，跟它說「照這份幫我把這支影片做成數位筆記」＋影片連結，它就會帶著你跑完。

---

## 0. 先看成果長什麼樣

- 線上分享首頁：<https://terry12260201.github.io/pumpkin-digital-notes/>
- 範例報告（登入平台影片）：李慕約「從 46 分寫到 90 分」
- 範例報告（YouTube）：把 AI 助手裝進 LINE

每份報告的樣子：**頁首基本資料 → 一句話大綱 → 大綱表 → 一段一段配截圖詳解（每張截圖標時間）→ 專有名詞小辭典**。手機、電腦都好讀。

---

## 1. 事前準備（電腦要有這些工具，只裝一次）

這套需要四個免費工具。在終端機（Mac 的「終端機」App）貼上檢查：

```bash
which ffmpeg ffprobe yt-dlp python3
python3 -c "import PIL; print('PIL ok')"
```

沒有的話，Mac 用 Homebrew 安裝：

```bash
brew install ffmpeg yt-dlp python3
python3 -m pip install pillow
```

- **ffmpeg / ffprobe**：處理影片、每隔幾秒截一張圖。
- **yt-dlp**：下載 YouTube 影片和字幕。
- **Python + Pillow(PIL)**：做縮圖總表、壓縮圖片。
- （選配）**Whisper**：影片沒字幕時，自己把聲音轉成文字。`pip install openai-whisper`

---

## 2. 三種輸入，走不同流程

| 你手上的東西 | 走哪條 |
|---|---|
| 🔗 YouTube 連結 | **流程 A**（全自動） |
| 🎬 電腦裡的錄影檔（.mov/.mp4） | **流程 A**（把連結換成檔案路徑） |
| 🔒 要登入才看得到的平台影片（如 gaiconf 線上課程） | **流程 B**（半自動） |

---

## 3. 流程 A：YouTube／錄影檔（全自動）

### 步驟 1：下載＋抽格＋抓字幕

把附錄的 `prep_youtube.py` 存到電腦，然後：

```bash
python3 prep_youtube.py "https://www.youtube.com/watch?v=xxxx" 10
# 或錄影檔：python3 prep_youtube.py "/Users/你/Movies/演講.mov" 10
```

（最後的 `10` = 每 10 秒截一張圖）跑完會在旁邊產生 `yt_job/` 資料夾，裡面有：
- `frames/` — 一張張截圖
- `contact_*.jpg` — 縮圖總表（一頁看很多張）
- `subs.txt` — 逐字稿（含時間）

### 步驟 2：讓 Claude 讀圖＋讀字幕，挑重點

跟 Claude 說：「讀 `yt_job/contact_*.jpg` 和 `yt_job/subs.txt`，挑出 12–18 張最有資訊量的畫面，記下每張的時間和它在講什麼。」

### 步驟 3：寫成白話報告

Claude 依「附錄的報告設計規格」把內容改寫成白話、配上截圖、加專有名詞辭典，產出 HTML。

---

## 4. 流程 B：要登入的平台影片（半自動）

> 重點：**不下載原始影片檔**（那些平台多半加密），改用「借你的登入狀態 + 在瀏覽器裡把影片跳到各時間點截圖 + 抓平台內建字幕」。全程在背景，不會佔用你的螢幕。

用的是 gstack 的 `/browse`（一個給 AI 用的無頭瀏覽器）。步驟：

1. **你先在自己的 Chrome 登入那個平台**（例如 gaiconf）。
2. Claude 執行 `browse cookie-import-browser` → 會打開一個「Cookie 選擇器」網頁。
3. **你在選擇器裡**：點你登入用的那個帳號（profile）→ 搜尋那個網站的網域 → 勾選匯入。
4. Claude 進課程頁，確認看得到內容。
5. Claude 找影片的**內建字幕**（很多平台有，例如繁中 VTT）當逐字稿——不用自己轉。
6. Claude 讓影片**靜音播放**，把時間一格一格往前跳、每 20 秒截一張圖（**一定要邊播邊截，暫停會蓋一張海報**）。
7. 之後同流程 A：挑重點 → 寫白話報告。

（給 Claude 的技術細節在附錄 B。）

---

## 5. 報告長什麼樣（設計規格：要「一模一樣」就照這個）

這是讓每份報告風格一致的關鍵。Claude 產 HTML 時要遵守：

### 內容結構（由上到下）
1. **頁首（深綠漸層卡）**：大標題 ＋ 基本資料格（講題／講者或頻道／來源連結／影片總長／上傳日／**閱讀日期**）＋ 一句話大綱。
2. **目錄**（可點跳）。
3. **大綱總結表**（30 秒看完：步驟／白話說的話／時間）。
4. **一段一段詳解**：每段標題 ＋ 白話說明 ＋ **1–2 張截圖**，每張截圖左邊有**綠色時間膠囊**（YouTube 就做成點了跳到影片那一秒的連結；平台影片連到課程頁）。
5. **專有名詞小辭典**（放最底部）：內文出現的每個專有名詞，這裡用白話解釋；內文的詞加**底線＋❓**，點了跳到辭典對應那列。
6. **頁尾**：這份報告怎麼來的（一句話）。

### 文字風格（最重要）
- **國小生也懂**。多用生活比喻。例：Webhook＝門鈴、ngrok＝臨時門牌、API＝通關密語、Agent＝會做事的小幫手。
- 專業內容照講，但不要用工程腔。看到專有名詞就丟進辭典。

### 版型與字體（CSS 規格）
- 字型：`-apple-system, "PingFang TC", "Noto Sans TC", "Microsoft JhengHei", sans-serif`
- 字級 17px、行高 1.9；閱讀寬度上限 **820px**（一行不會太長）。
- 主色（teal）：`#0e6e63`／深 `#0b5a51`；背景 `#f5f8f8`；卡片白底、圓角 16px、細邊框 `#e0e9e8`。
- 頁首：`linear-gradient(135deg,#0b3f39,#128577)` 深綠漸層、白字。
- 雙欄圖片在手機（<640px）自動收成單欄；表格可橫向滑動；圖片 `max-width:100%`。
- 時間膠囊：teal 底白字圓角小標籤，前面加 `▶`。
- 專有名詞連結：teal 虛線底＋小問號。
- 深色相容可省略；務必給 body 明確底色。

> 附錄 C 是一支「報告產生器骨架」（Python），把截圖、時間、辭典、白話文字填進去就會吐出符合上面規格的單檔 HTML（圖片用 base64 內嵌，一個檔案就能到處傳）。

### 品質驗證（交付前一定做）
- 每張截圖對應正確段落、時間準。
- 手機（390px 寬）＋電腦都不破圖、不會左右溢出。
- 用 gstack `/browse` 起一個本機 http server 開來看（直接開 file:// 會被擋），桌面＋手機各截圖確認。

---

## 6. 交付：兩個版本 + 上線 + 存檔

**版本① HTML 報告** — 上面那份，可直接分享。
**版本② 筆記 .md** — 給 Obsidian 第二大腦用：frontmatter ＋ 摘要 ＋ **重點時間清單** ＋ 核心觀念 ＋ 連回 HTML 與線上網址。

**上線分享**（產生一個可傳給別人的連結）：把報告丟進分享庫、跑 `publish.py`，它會自動更新首頁索引並上傳，最後印出網址。（附錄 D）

**存進筆記庫（依平台分資料夾）**：`報告.html` ＋ `筆記.md` 放進 `影片筆記/<平台資料夾>/<日期_主題>/`。
平台資料夾例如：`Gaiconf 數位教學平台`、`YT影片整理`，其他平台就再開一個對應資料夾，之後持續加入——**相關平台的都整理進對應資料夾**。並在 `_索引.md` 加一列。

---

## 7. 給 Ann 的「一鍵咒語」（複製這段丟給你的 Claude）

```
我要用「小南瓜數位筆記」的方法把一支影片做成白話圖文報告。
這份手冊我一起給你（貼上本檔），請你嚴格照裡面的「報告設計規格」做。

我的輸入是：<貼 YouTube 連結／或說「我有一個錄影檔在 <路徑>」／或「這是要登入的平台影片：<網址>，我已經登入好了」>

請你：
1. 依流程 A 或 B 把影片抽成截圖＋逐字稿。
2. 挑 12–18 張重點截圖，寫成國小生也讀得懂的白話報告（含頁首基本資料、大綱、
   一段段配截圖詳解並標時間、專有名詞小辭典），版型字體照手冊規格。
3. 產出單檔 HTML，並用瀏覽器起本機 server 驗證手機＋電腦都不破圖，給我截圖看。
先跟我確認你讀懂手冊了，再開始。
```

---

## 附錄 A：`prep_youtube.py`（下載＋抽格＋字幕）

> 完整腳本在 GitHub：<https://github.com/terry12260201/pumpkin-digital-notes>（`小南瓜數位筆記` skill 的 `scripts/` 裡也有一份）。
> 這支負責機械工：下載影片、抓官方字幕、每 N 秒抽一張關鍵幀、做縮圖總表、把字幕轉成帶時間的純文字。

## 附錄 B：平台影片（流程 B）給 Claude 的技術要點
- 影片常在 `<iframe>`（如 loopix），`<video>` 是 `blob:`（加密串流，**不下載**）。
- 字幕：切進 iframe 讀 `document.querySelector('track').src`（如 kaik.io 的 VTT），`curl` 抓下來。
- 抽格：進 iframe `video.muted=true; video.play()`；迴圈 `video.currentTime=<t>` → 等約 2.2 秒 → 回主框架
  `browse screenshot --clip <iframe座標> /tmp/frames/t_<秒>.jpg`（**只能存到 /tmp 或家目錄**）。
- iframe 座標 = 主框架 `iframe.getBoundingClientRect()`。

## 附錄 C：報告產生器骨架
> 見 skill `scripts/report_template.py`：定義好 CSS（上面的規格）、`GLOSSARY` 辭典、`fig()`（截圖＋可點時間）、
> `T()`（內文詞連辭典），把你挑的截圖與白話文字填進各 `<section>` 即可輸出單檔 HTML。

## 附錄 D：`publish.py`（一鍵上線）
> 見 skill `scripts/publish.py`：把 `報告.html` 複製到分享庫的分類資料夾、更新 `reports.json`、
> 重建首頁 `index.html`、git commit + push，最後印出線上網址。

---

*本手冊由「小南瓜數位筆記」skill 產出並維護。完整腳本與最新版：GitHub `pumpkin-digital-notes`、本機 `~/.claude/skills/pumpkin-digital-notes/`。*
